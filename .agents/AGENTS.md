# Agent Rules

- **Code Formatting**: ALWAYS run `go fmt ./...` and ensure there are no formatting errors before committing or pushing any code.
- **Testing**: ALWAYS run `go test ./...` to verify tests pass before raising a PR.
